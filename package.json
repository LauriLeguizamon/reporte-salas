{
  "name": "reporte-salas",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "seed": "pnpm --filter @reporte-salas/api run seed",
    "build": "pnpm -r run build"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
